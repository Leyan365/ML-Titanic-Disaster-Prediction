# Titanic-ML-Prediction

A machine learning pipeline for the Titanic dataset. Includes data preprocessing, feature engineering, SMOTE oversampling, and model comparison (Logistic Regression, Random Forest, XGBoost).  

Work in progress — test set prediction and submission steps coming soon.
